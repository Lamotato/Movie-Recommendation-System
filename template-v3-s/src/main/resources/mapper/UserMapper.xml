<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.platform.mapper.UserMapper">
    <select id="queryPage" resultType="com.project.platform.entity.User">
        SELECT user.*
        FROM user
        <include refid="queryConditions"/>
        ORDER BY user.id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="queryCount" resultType="int">
        SELECT count(user.id) FROM user
        <include refid="queryConditions"/>
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultType="com.project.platform.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 根据用户名（邮箱）查询 -->
    <select id="selectByUserName" resultType="com.project.platform.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 根据邮箱查询 -->
    <select id="selectByEmail" resultType="com.project.platform.entity.User">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <!-- 根据手机号查询 -->
    <select id="selectByTel" resultType="com.project.platform.entity.User">
        SELECT * FROM user WHERE tel = #{tel}
    </select>

    <!-- 查询所有 -->
    <select id="list" resultType="com.project.platform.entity.User">
        SELECT * FROM user ORDER BY id DESC
    </select>

    <sql id="queryConditions">
        <where>
            <if test="query.username != null and query.username.trim() != ''">
                AND user.username LIKE CONCAT('%', #{query.username}, '%')
            </if>
            <if test="query.email != null and query.email.trim() != ''">
                AND user.email LIKE CONCAT('%', #{query.email}, '%')
            </if>
            <if test="query.tel != null and query.tel.trim() != ''">
                AND user.tel LIKE CONCAT('%', #{query.tel}, '%')
            </if>
            <if test="query.status != null and query.status.trim() != ''">
                AND user.status = #{query.status}
            </if>
        </where>
    </sql>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.project.platform.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username != null">username,</if>
            <if test="password != null">password,</if>
            <if test="nickname != null">nickname,</if>
            <if test="avatarUrl != null">avatar_url,</if>
            <if test="tel != null">tel,</if>
            <if test="email != null">email,</if>
            <if test="gender != null">gender,</if>
            <if test="birthday != null">birthday,</if>
            <if test="status != null">status,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username != null">#{username},</if>
            <if test="password != null">#{password},</if>
            <if test="nickname != null">#{nickname},</if>
            <if test="avatarUrl != null">#{avatarUrl},</if>
            <if test="tel != null">#{tel},</if>
            <if test="email != null">#{email},</if>
            <if test="gender != null">#{gender},</if>
            <if test="birthday != null">#{birthday},</if>
            <if test="status != null">#{status},</if>
        </trim>
    </insert>

    <!-- 更新 -->
    <update id="updateById" parameterType="com.project.platform.entity.User">
        UPDATE user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="nickname != null">
                nickname = #{nickname},
            </if>
            <if test="avatarUrl != null">
                avatar_url = #{avatarUrl},
            </if>
            <if test="tel != null">
                tel = #{tel},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID列表删除 -->
    <delete id="removeByIds">
        DELETE FROM user
        WHERE id IN
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
